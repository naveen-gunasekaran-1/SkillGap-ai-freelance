import { Text, View } from 'react-native';

export default function TabsHomeScreen(): React.JSX.Element {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Mobile tabs scaffold</Text>
    </View>
  );
}
