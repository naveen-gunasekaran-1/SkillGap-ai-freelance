import { Text, View } from 'react-native';

export default function LoginScreen(): React.JSX.Element {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Login screen scaffold</Text>
    </View>
  );
}
