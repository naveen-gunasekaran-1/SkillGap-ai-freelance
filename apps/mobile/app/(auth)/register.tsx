import { Text, View } from 'react-native';

export default function RegisterScreen(): React.JSX.Element {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Register screen scaffold</Text>
    </View>
  );
}
